# Suitemart

The instructions for this repository are in **`AGENTS.md`** — read it in full
before writing any code. They are kept in one file so that every assistant works
to the same contract; this file exists only because Gemini looks for it by name.

Then read **`docs/HANDOFF.md`** for the current state of the work and what to do
next.
